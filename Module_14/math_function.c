#include<stdio.h>
#include<math.h>
#include<stdlib.h>

int main()

{
    double x,y;
    scanf("%lf %lf",&x,&y);
    printf("%lf\n",ceil(x));
    // printf("%lf\n",floor(x));
    // printf("%lf\n",round(x));
    // printf("%lf\n",sqrt(x));
    // printf("%lf\n",pow(x,y));
    // printf("%lf\n",abs(x));
    return 0;
}